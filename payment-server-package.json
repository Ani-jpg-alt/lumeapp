{
  "name": "independent-payment-server",
  "version": "1.0.0",
  "description": "Independent Yoco Payment Intent Server - Can be used with any frontend application",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "install-deps": "npm install express cors node-fetch dotenv",
    "setup": "npm install && cp server.env.example .env",
    "test": "echo \"Test with: curl http://localhost:3001/health\" && exit 0"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "node-fetch": "^2.7.0",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "keywords": [
    "yoco",
    "payments",
    "payment-gateway",
    "api",
    "server",
    "independent",
    "south-africa"
  ],
  "author": "Payment Server",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "standalone-payment-server"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}